import { Flex } from "@mantine/core";

type Props = {};

const Strategies = ({}: Props) => {
  return (
    <Flex
      direction="column"
      sx={(theme) => ({
        width: "90%",
        margin: "auto",
      })}
    ></Flex>
  );
};

export default Strategies;
